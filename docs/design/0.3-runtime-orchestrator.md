# 0.3 Runtime Orchestrator Design

## Goal

Milestone 3 layers durable orchestration on top of the runtime foundations without introducing real IM providers or real agent CLI adapters yet. The runtime should own cross-platform ordering, duplicate handling, acknowledgement preconditions, queueing, recovery, and policy decisions. Concrete providers and agents remain behind adapter boundaries.

This design note records the M3 invariants as they are introduced across focused PRs. It is not a compatibility promise during early `0.x` development.

## Boundaries

Included:

- Durable inbound event ledger for duplicate delivery handling.
- Store-level ack-after-persist persistence primitive for inbound events.
- Initial runtime intake boundary that acknowledges an inbound delivery only after durable record or duplicate detection.
- Runtime-owned event identity rules for provider/scope-safe duplicate detection.
- Process-local serialization for same-path `StateStore` read-modify-write helpers.
- Durable outbound outbox records and store-level enqueue-before-send persistence primitive.
- Outbox consumption state primitives for claiming queued deliveries and recording delivered or failed attempts.
- Later M3 queueing, outbox workers, concrete adapter handoff, startup recovery, and policy skeletons.

Excluded:

- Real Lark / Feishu, Slack, Discord, or other provider transports.
- Real agent process execution.
- Cross-process queueing or cross-process `StateStore` writer coordination beyond daemon ownership.
- A full platform SDK abstraction.

## Inbound Delivery Intake

Inbound transports may require an explicit delivery acknowledgement. The runtime must persist or de-duplicate the normalized event before the adapter acknowledges the platform delivery.

The initial shape is:

```text
InboundDelivery -> RuntimeOrchestrator::accept_inbound_delivery -> ImAdapter::acknowledge_inbound_delivery
```

`RuntimeOrchestrator::accept_inbound_delivery` records the event through `StateStore::persist_inbound_event`. When persistence returns `Recorded` or `Duplicate`, the orchestrator constructs an acknowledgement object and passes it to the adapter. If persistence fails, the adapter is not called and the platform delivery remains unacknowledged so the transport can retry according to provider semantics. If adapter acknowledgement fails, the error is reported after the event is already durable.

The acknowledgement value is created by the crate, not by external callers, so adapters cannot forge an acknowledgement that skipped runtime persistence.

## Event Identity

`EventId` is the duplicate-detection key. Provider delivery identifiers are often unique only within a provider, app, tenant, chat, channel, or receive stream, so adapters must namespace inbound delivery ids before handing events to the runtime.

`EventId::for_platform_delivery(platform, scope, delivery_id)` provides the default canonical shape for this namespace. The runtime treats the resulting id as opaque after construction.

## StateStore Safety

M3 introduces read-modify-write helpers that can be reached through cloned or independently constructed `StateStore` handles. Same-path handles therefore share a process-local write lock, and the store normalizes path aliases before choosing both the lock key and the I/O path. This prevents two in-process inbound deliveries from both being acknowledged while one ledger entry is lost to last-writer-wins replacement.

`StateStore::save` preserves inbound ledger entries already present on disk when saving an older in-memory snapshot. It fails closed if the existing file cannot be read, decoded, or validated, because overwriting invalid state could otherwise erase an already acknowledged delivery.

The same stale-save protection applies to outbound outbox records. A stale in-memory snapshot must not erase a delivery that was already durably queued by another same-path `StateStore` handle. If the same outbound delivery id appears with different payload or state, or if preserving an existing outbound delivery would require a session that the candidate state does not contain, saving fails closed instead of choosing a last-writer-wins record or replacing the existing file.

This lock is not a cross-process queue. The daemon lifecycle keeps one owned runtime process today; later orchestrator work should make cross-process runtime coordination explicit if the project adds more writers.

## Outbound Outbox

Outbound replies are recorded in durable state before any future adapter is allowed to send them. `OutboundDeliveryRecord` stores the delivery id, target session, agent/system-authored message, delivery status, timestamps, attempt count, optional delivery time, and optional last error.

The initial outbox states are:

- `pending`: queued and not yet attempted.
- `delivering`: claimed for an attempted delivery.
- `delivered`: successfully handed to the future IM adapter.
- `failed`: attempted and failed, with a non-empty last error.

`StateStore::enqueue_outbound_delivery` performs a read-modify-write under the same path-local write lock used by inbound delivery persistence. It accepts only pending records for known sessions, returns `Queued` only after the delivery is durable, returns `Duplicate` for an identical already-queued record, and fails closed for conflicting ids or failed persistence.

`StateStore::claim_next_outbound_delivery` performs the next consumption step: it loads state under the path-local write lock, transitions the next pending or failed delivery to `delivering`, persists the claim, and only then returns the claimed record to the caller as the send handoff. `StateStore::mark_outbound_delivery_delivered` and `StateStore::mark_outbound_delivery_failed` likewise persist the terminal or retryable outcome before returning the updated record.

This slice does not yet implement an outbox worker, retry scheduler, backoff policy, or concrete IM send API. Those belong to later M3 queueing/orchestration and M5 provider-integration work. Until provider send APIs exist, the runtime-owned handoff is the durable `delivering` record returned after a successful claim.

## Adapter Surface

The initial `ImAdapter` capability is deliberately narrow: it only acknowledges a runtime-approved inbound delivery. Outbound delivery records are now durable runtime state, but concrete IM send capabilities and agent-side capabilities are introduced by later M3/M4 PRs.

Real provider transport details stay inside concrete IM adapters. The core runtime sees normalized events, acknowledgement tokens, and adapter results only.

## Later M3 Work

The ack intake boundary is only one M3 slice. The rest of the milestone still needs:

- Per-scope message queue with debounce and batching.
- Outbox worker, retry scheduling/backoff, and concrete outbound adapter handoff.
- Scope-level mutual exclusion for active runs.
- Startup recovery for pending, running, and failed runs.
- Workspace and access policy skeletons.
- Concrete runtime orchestration across storage, queues, policies, and adapter boundaries.

## Acceptance Notes

The inbound intake slice is complete when duplicate inbound deliveries are recorded or recognized before acknowledgement, persistence failures do not acknowledge, acknowledgement failures are reported after durability, same-process same-path store handles cannot lose acknowledged inbound records, stale saves preserve existing inbound ledger entries, and the architecture/roadmap documents state that real transports and cross-process orchestration remain out of scope.

The outbound outbox slice is complete when outbound records validate session/message ownership, state transitions reject invalid delivery shapes and backward timestamps, version 3 state files migrate with an empty outbox, version 4 state files require explicit outbound deliveries, enqueue accepts only pending records for known sessions and returns only after durable persistence, duplicate enqueue is idempotent, same-process same-path store handles cannot lose queued outbound records, and stale saves preserve already queued deliveries or fail closed without replacing the existing file when dependencies are missing.

The outbox consumption primitive slice is complete when a claim transitions exactly one pending or failed delivery to `delivering` before returning it, failed deliveries can be claimed again for retry, no delivery is returned when no record is claimable, same-process same-path store handles cannot claim the same delivery twice, failed claim persistence returns an error instead of a handoff, and delivered or failed outcomes are durable before the updated record is returned.
